# Ignore everything by default
*

# Keep git + core scripts
!.gitignore
!deploy.sh
!flipper_menu.sh

# Keep the backup script (but NOT backup contents)
!backups/
!backups/latest_backup.sh

# Keep only the code snapshot folder
!code/
!code/**

